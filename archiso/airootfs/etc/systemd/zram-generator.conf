# /etc/systemd/zram-generator.conf
# Configuration file for systemd zram-generator on Xray_OS

[zram0]
# Allocate zram equivalent to total RAM dynamically
zram-size = ram

# High-speed zstd compression algorithm
compression-algorithm = zstd

# Higher priority than disk swap
swap-priority = 100
